- add features to Parakeet
(retriever, chunker, ...)


chunker / guided chunker
- with a split tag
- at every split, read if meta data (with meta data delemiter)
- then create the chunk

=> then create embeddings


function to call
